# OpenSurv general-builder run 1

## Actions
- Skimmed doc/repo-understanding briefs for current architecture expectations and watchdog flow.
- Reviewed monitor layout logic (Screen/Stream) and Windows DPI handling to trace undersized MPV windows.
- Added logging import plus robust DPI scale fallbacks (GetScaleFactorForMonitor, GetDpiForSystem, GetDeviceCaps) in surveillance/surveillance.py so Windows monitor_scale is populated even when GetDpiForMonitor fails.

## Verification
- Not run (no automated test suite specified; unable to execute mpv/camera stack in this environment).
