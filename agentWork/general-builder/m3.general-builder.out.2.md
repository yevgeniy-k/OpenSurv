# OpenSurv general-builder run 2

## Actions
- Reviewed surveillance/logs/main.log to confirm Windows build still reports monitor 131073 at 1920x1080 while the physical display is 2560x1440, indicating DPI scaling is masking the true pixel size.
- Extended surveillance/surveillance.py `_get_monitor_dpi_scale` to accept the per-monitor HDC from `EnumDisplayMonitors` and added a LOGPIXELSX fallback so we can still compute a DPI ratio when shcore APIs return 96 DPI.
- Wired the new HDC parameter through `get_monitors()` so each Screen receives a non-1.0 `dpi_scale`, which should let `_convert_to_mpv_coordinates` request the correct mpv geometry.

## Verification
- Not run (mpv/camera wall not executable in this environment).
