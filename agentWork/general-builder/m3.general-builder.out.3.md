# OpenSurv general-builder run 3

## Actions
- Investigated `_convert_to_mpv_coordinates` in surveillance/core/Stream.py and confirmed we were still multiplying every Windows coordinate by `monitor_scale`, even though EnumDisplayMonitors already reports physical pixels (the user reiterated monitor 131073 is 1920x1080).
- Removed the Windows-only scaling block so mpv receives the raw quadrant geometry; this should eliminate the undersized windows while keeping future support for per-monitor DPI if we need it elsewhere.

## Verification
- Not run (mpv/camera wall cannot be launched inside this environment).
