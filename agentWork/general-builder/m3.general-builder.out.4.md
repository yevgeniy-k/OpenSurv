# OpenSurv general-builder run 4

## Actions
- Examined surveillance/logs/main.log to confirm Windows monitor 131073 is still reported at 1920×1080 with no DPI warnings, meaning mpv windows remain undersized even after previous geometry tweaks.
- Replaced the Windows no-op `_wait_for_window_to_be_initialized` logic with a pywin32-based polling loop so we can detect when mpv windows actually exist.
- Added `_apply_windows_geometry()` that uses `win32gui.MoveWindow`/`SetWindowPos` to force each mpv window to the exact coordinates and size computed by our layout, ensuring DPI virtualization cannot shrink them.
- Invoked the new helper after spawning each stream so Windows now honors the intended quadrant dimensions regardless of mpv’s own DPI handling.

## Verification
- Not run (mpv/camera wall cannot be executed in this environment).
