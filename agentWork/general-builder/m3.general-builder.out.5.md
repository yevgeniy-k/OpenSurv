# OpenSurv general-builder run 5

## Actions
- Addressed intermittent visibility of Windows mpv windows by making `_apply_windows_geometry()` reassert window visibility and placement three times with short delays, using `ShowWindow` + `SetWindowPos` to counter race conditions where mpv moves its own window after launch.

## Verification
- Not run (mpv/camera wall not executable in this environment).
