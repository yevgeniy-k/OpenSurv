# Repo Understanding Run 1 (2026-01-21)

## Inputs
- README, install script, and core surveillance modules (`surveillance.py`, `core/ScreenManager.py`, `core/Screen.py`, `core/Stream.py`, `core/util/draw.py`, YAML configs).

## Findings
- Documented control flow from monitor detection through screen rotation and watchdog maintenance.
- Captured data model relationships (ScreenManager → Screen → Stream) and YAML-driven configuration knobs.
- Noted Linux-specific assumptions (wmctrl, ffprobe paths) that may block Windows parity despite recent pywin32 additions.

## Artifacts
- Published summary: `doc/repo-understanding/2026-01-21-overview.md`.

## Open Questions / Next Steps
1. Validate how watchdog restarts behave on Windows without `os.setsid`/process group killing.
2. Identify replacements for wmctrl/xdotool when running outside X11.
3. Extend documentation with install/runtime services (LightDM, systemd units) for end-to-end ops view.
